{
  "name": "my-personal-starter-react-setup",
  "version": "3.0.0",
  "private": true,
  "engines": {
    "node": ">=8.0.0"
  },
  "main": "server.ts",
  "scripts": {
    "start": "cd server && yarn start",
    "client:dev": "cd client && yarn start",
    "server:dev": "cd server && yarn start:dev",
    "dev": "concurrently --kill-others-on-fail \"yarn server:dev\" \"yarn client:dev\"",
    "rebuild": "yarn delete-dependencies && yarn && cd client && yarn && cd .. && cd server && yarn",
    "nuke-dependencies": "rm -rf node_modules rm -f yarn.lock && cd client && rm -rf node_modules && rm -f yarn.lock && cd .. && cd server && rm -rf node_modules && rm -f yarn.lock",
    "delete-dependencies": "rm -rf node_modules && cd client && rm -rf node_modules && cd .. && cd server && rm -rf node_modules",
    "dangerously-rebuild": "yarn nuke-dependencies && yarn && cd client && yarn && cd .. && cd server && yarn",
    "generate": "cd client && yarn generate",
    "force-build": "cd client && FORCE_BUILD=1 yarn build",
    "initdb": "babel-node setup/initdb.js",
    "dumpdb": "babel-node setup/dumpdb.js",
    "force-publish": "heroku pg:reset && heroku pg:push my-local-database-name DATABASE_URL --app my-heroku-app-name; git push heroku master",
    "resolve": "yarn && cd client && yarn && cd .. && cd server && yarn"
  },
  "dependencies": {
    "@babel/core": "^7.0.0",
    "@babel/node": "^7.2.2",
    "@babel/preset-env": "^7.0.0",
    "@babel/register": "^7.0.0",
    "@babel/runtime": "^7.0.0",
    "@overnightjs/core": "^1.6.14",
    "@overnightjs/logger": "^1.1.10",
    "@types/bluebird": "^3.5.29",
    "@types/dotenv": "^8.2.0",
    "@types/express": "^4.17.2",
    "@types/node": "^13.7.4",
    "@types/validator": "^12.0.1",
    "chalk": "^2.4.2",
    "concurrently": "^4.1.0",
    "dotenv": "^7.0.0",
    "express": "^4.16.4",
    "find-process": "^1.4.1",
    "isomorphic-fetch": "^2.2.1",
    "path": "^0.12.7",
    "pg": "^7.8.1",
    "sequelize": "^5.21.4",
    "sequelize-typescript": "^1.1.0",
    "ts-node": "^8.6.2",
    "typescript": "~3.7.2",
    "yargs": "^13.2.1"
  },
  "devDependencies": {
    "husky": "^2.3.0"
  }
}
